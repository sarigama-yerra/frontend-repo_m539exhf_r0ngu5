import React from "react";

function Header() {
  return (
    <header className="py-8 text-center">
      <h1 className="text-4xl font-bold text-white tracking-tight">Calculator</h1>
      <p className="text-blue-200/80 mt-2">A clean, responsive calculator built with HTML, CSS, and JavaScript (via React)</p>
    </header>
  );
}

export default Header;
